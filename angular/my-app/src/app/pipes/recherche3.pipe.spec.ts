import { Recherche3Pipe } from './recherche3.pipe';

describe('Recherche3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Recherche3Pipe();
    expect(pipe).toBeTruthy();
  });
});
