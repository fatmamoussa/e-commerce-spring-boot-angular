import { Recherche2Pipe } from './recherche2.pipe';

describe('Recherche2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Recherche2Pipe();
    expect(pipe).toBeTruthy();
  });
});
